/** @type {import('next').NextConfig} */
const nextConfig = {
    devIndicators: {
      buildActivity: false, // Disables activity indicator
      autoPrerender: false, // Disables pre-render indicator
    },
  };
  
  export default nextConfig;
  